<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ce.evalution.ce_evaluation">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".result.Choose_grade_subject">
        </activity>
        <activity android:name=".Update_Grade">
        </activity>
        <activity android:name=".Menu_teacher">
        </activity>
        <activity android:name=".Filter.Filter_subject"/>
        <activity
                android:name=".result.Menu_report"
                android:label="@string/app_name1"
                android:theme="@style/AppThemeNoActionBar">
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".result.Report_subject"/>
        <activity android:name=".result.Report_student"/>
        <activity android:name=".dashboard.Dashboard"/>
        <activity android:name=".evaluation.List_component"/>
        <activity android:name=".evaluation.List_subject"/>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>